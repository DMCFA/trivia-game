import react from "react";
import { Button } from "react-bootstrap";

export const BackToSelectionBtn = () => {
  return (
    <div>
      <Button color="warning" size="lg">
        Go Back!
      </Button>
    </div>
  );
};
